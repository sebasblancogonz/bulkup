import SwiftUI

extension Color {
    /// BulkUp accent (lime #84CC16) and brand graphite (#111827).
    static let lime = Color(red: 0.518, green: 0.800, blue: 0.086)
    static let graphite = Color(red: 0.067, green: 0.094, blue: 0.153)
    /// Subtle elevated-card fill that reads on the watch's black background.
    static let card = Color.white.opacity(0.07)
}
